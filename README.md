# industrial-data-collector

IoT telemetry system: MQTT and Modbus TCP data pipelines with a Django REST
API, storing sensor data in MongoDB and device metadata in PostgreSQL.
Fully containerized with Docker Compose.

_Backend assignment. Work in progress._
